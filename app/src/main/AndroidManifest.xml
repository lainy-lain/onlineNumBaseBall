<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pnu.termproject.onlinenumbaseball">
    <!-- 인터넷 사용 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineNumBaseball">

        <service
            android:enabled="true"
            android:name=".MultiRoom$ClosingService"
            android:exported="false"
            android:stopWithTask="false" />

        <activity android:name=".MultiRoom" />
        <activity android:name=".LeaderBoardActivity" />
        <activity android:name=".SingleRankingUpdateActivity" />
        <activity android:name=".Setting" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Game" />
        <activity android:name=".MultiList" />
    </application>

</manifest>